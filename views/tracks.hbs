<div class="tracks">
    <ol>
        {{#each tracks}}
        
        <li>
            {{this.name}}
            <audio controls src="{{this.preview_url}}"> Your browser does not support the <code>audio</code> element. 
            </audio>
        </li>
         
        {{/each}}
    </ol>    
</div>
